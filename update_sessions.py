import json,os,string,random;def generate_conversation_id(): return "".join(random.choices(string.ascii_uppercase + string.digits, k=6));def update_session_file(filepath): data = json.load(open(filepath));data["conversation_id"] = data.get("conversation_id", generate_conversation_id());print(f"Updated {os.path.basename(filepath)} with conversation_id {data[\"conversation_id\"]}");json.dump(data, open(filepath, "w"), indent=2);[update_session_file(os.path.join("sessions", f)) for f in os.listdir("sessions") if f.endswith(".json") and f != ".gitkeep"]
